<%= render "shared/track_show", track: @track %>

<article class="track comments">
  <%= render "shared/comments_show", comments: @track.comments %>
  <%= render "shared/comments_form", track_id: @track.id %>
</article>